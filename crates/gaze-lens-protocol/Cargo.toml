[package]
name = "gaze-lens-protocol"
version = "0.0.0"
edition = "2024"
rust-version = "1.89"
publish = false
license = "Apache-2.0"
description = "Closed gaze-lens-source/2 DTOs and bounded framing; development only"

[package.metadata.dist]
dist = false

[dependencies]
serde.workspace = true
serde_json.workspace = true
base64.workspace = true

[lints]
workspace = true
